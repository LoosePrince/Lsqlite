# 简单博客 Demo

这是一个静态博客演示，数据写入当前项目实际部署的 Lsqlite 服务。

功能范围：

- 发帖
- 点赞
- 评论
- 文章管理
- 评论管理
- 后台隐藏、恢复、删除

## 运行

在项目根目录执行：

```bash
node demo/server.mjs
```

然后访问：

```text
http://localhost:35002
```

说明：浏览器直接打开 `index.html` 时会遇到远程服务 CORS 限制，因此推荐使用上面的本地服务。该服务只做两件事：提供静态页面，并把 `/api/*` 请求转发到实际部署地址。